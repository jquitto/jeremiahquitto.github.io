apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: sre-demo
spec:
  selector:
    app: sample-app
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
